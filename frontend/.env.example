# For local development — CRA proxy handles /api calls to localhost:3000
# For production deployment, set this to your deployed backend URL:
# REACT_APP_API_URL=https://your-backend.onrender.com/api
REACT_APP_API_URL=
